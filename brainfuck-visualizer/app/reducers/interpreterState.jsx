const {createReducer} = require('./reducer');

const InterpreterState = require('../models/interpreterState');

const {
  ACTION_RELOAD
} = require('../actions/InterpreterActions');

module.exports = createReducer(null, {
});
